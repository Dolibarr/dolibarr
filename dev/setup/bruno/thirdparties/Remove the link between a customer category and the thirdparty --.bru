meta {
  name: Remove the link between a customer category and the thirdparty --
  type: http
  seq: 14
}

delete {
  url: {{base_url}}/thirdparties/{id}/categories/{category_id}
  body: none
  auth: none
}
