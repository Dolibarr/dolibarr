meta {
  name: Remove the link between a category and the thirdparty --
  type: http
  seq: 17
}

delete {
  url: {{base_url}}/thirdparties/{id}/supplier_categories/{category_id}
  body: none
  auth: none
}
