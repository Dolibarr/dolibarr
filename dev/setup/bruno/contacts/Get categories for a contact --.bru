meta {
  name: Get categories for a contact --
  type: http
  seq: 8
}

get {
  url: {{base_url}}/contacts/{id}/categories
  body: none
  auth: none
}

query {
  ~sortfield:
  ~sortorder:
  ~limit:
  ~page:
}
