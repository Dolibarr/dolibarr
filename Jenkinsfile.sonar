pipeline {
   agent {
    kubernetes {
      yaml '''
        apiVersion: v1
        kind: Pod
        spec:
          containers: 
          - name: trivy
            image: aquasec/trivy:canary
            command:
            - cat
            tty: true
        '''
    }
  }
  stages {
   stage('trivy scan'){
      steps{
          container('trivy'){
            sh "trivy image iyedbnaissa/dolibarr_build:30 --output ${WORKSPACE}/trivy_report.json --scanners vuln"
            sh "pwd"
            sh "cat ${WORKSPACE}/trivy_report.json"
          }
      }
   }
    stage('SonarQube Analysis') {
      environment {
             SONAR_SCANNER_OPTS = "-Xmx4096m -Xmx512m"
      }
      steps {
        script {
          def scannerHome = tool 'SonarQube_Scanner';
          // Execute SonarQube analysis
          withSonarQubeEnv('SonarQube_Server') {
           
            sh "${scannerHome}/bin/sonar-scanner -X -Dproject.settings=sonar-project.properties"
          }
        }
      }
    }
  }
}
